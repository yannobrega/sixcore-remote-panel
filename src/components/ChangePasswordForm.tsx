"use client";
import { useState } from "react";import { useRouter } from "next/navigation";
export function ChangePasswordForm(){const [message,setMessage]=useState("");const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMessage("");const f=new FormData(e.currentTarget);const np=String(f.get("newPassword")||""),confirm=String(f.get("confirm")||"");if(np!==confirm){setMessage("As novas senhas não coincidem.");return}const res=await fetch("/api/auth/change-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({currentPassword:f.get("currentPassword"),newPassword:np})});if(!res.ok){setMessage("Não foi possível alterar a senha. Verifique a senha atual e use no mínimo 12 caracteres.");return}r.replace("/login");r.refresh()}return <form className="form-grid" onSubmit={submit}><label>Senha atual<input name="currentPassword" type="password" required/></label><span/><label>Nova senha<input name="newPassword" type="password" minLength={12} required/></label><label>Confirmar nova senha<input name="confirm" type="password" minLength={12} required/></label>{message&&<div className="error full">{message}</div>}<div className="full"><button className="primary">Alterar senha e encerrar sessões</button></div></form>}
