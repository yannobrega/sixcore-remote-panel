"use client";
import { useState } from "react";import { useRouter } from "next/navigation";
export function CompanyForm(){const [error,setError]=useState("");const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const res=await fetch("/api/companies",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({legalName:f.get("legalName"),tradeName:f.get("tradeName"),taxId:f.get("taxId"),contractActive:f.get("contractActive")==="on",notes:f.get("notes")})});if(!res.ok){setError("Não foi possível cadastrar. Verifique duplicidade e campos.");return}form.reset();r.refresh()}return <form className="form-grid" onSubmit={submit}><label>Razão social<input name="legalName" required /></label><label>Nome fantasia<input name="tradeName" required /></label><label>CNPJ/CPF<input name="taxId" /></label><label className="check"><input name="contractActive" type="checkbox" defaultChecked /> Contrato ativo</label><label className="full">Observações<textarea name="notes" rows={3}/></label>{error&&<div className="error full">{error}</div>}<div className="full"><button className="primary">Cadastrar empresa</button></div></form>}
