import { requireUser } from "@/server/auth/session";import { db } from "@/server/db/client";import { AppShell } from "@/components/AppShell";import { UserForm } from "@/components/UserForm";import { ToggleButton } from "@/components/ToggleButton";
export const dynamic="force-dynamic";
export default async function Users(){const u=await requireUser(["admin"]);const r=await db().query(`SELECT id,name,email,role,active,created_at FROM users WHERE deleted_at IS NULL ORDER BY name`);return <AppShell user={u}><header className="page-head"><div><span className="eyebrow">Administração</span><h1>Usuários</h1><p>Perfis e acesso ao painel.</p></div></header><section className="panel"><h2>Novo usuário</h2><UserForm/></section><section className="panel"><div className="table-wrap"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Perfil</th><th>Status</th><th></th></tr></thead><tbody>{r.rows.map((x:any)=><tr key={x.id}><td><strong>{x.name}</strong></td><td>{x.email}</td><td><span className="badge neutral">{x.role}</span></td><td>{x.active?"Ativo":"Inativo"}</td><td>{x.id!==u.id&&<ToggleButton endpoint={`/api/users/${x.id}`} active={x.active}/>}</td></tr>)}</tbody></table></div></section></AppShell>}
