import { NextResponse } from "next/server";import { currentUser } from "@/server/auth/session";import { assertSameOrigin } from "@/server/http/request";import { db } from "@/server/db/client";import { audit } from "@/server/audit/write";
export async function PATCH(request:Request,ctx:{params:Promise<{id:string}>}){try{assertSameOrigin(request)}catch{return NextResponse.json({error:"origin_not_allowed"},{status:403})}const u=await currentUser();if(!u)return NextResponse.json({error:"unauthorized"},{status:401});if(u.role==="viewer")return NextResponse.json({error:"forbidden"},{status:403});const {id}=await ctx.params;const b=await request.json().catch(()=>null) as any;if(typeof b?.active!=="boolean")return NextResponse.json({error:"invalid_request"},{status:400});const r=await db().query(`UPDATE devices SET active=$1,updated_at=now() WHERE id=$2 AND deleted_at IS NULL RETURNING company_id`,[b.active,id]);if(!r.rows[0])return NextResponse.json({error:"not_found"},{status:404});await audit({actorUserId:u.id,companyId:r.rows[0].company_id,deviceId:id,action:"device.update_status",outcome:"success",metadata:{active:b.active}});return NextResponse.json({ok:true})}
